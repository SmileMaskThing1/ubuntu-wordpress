https://github.com/SmileMaskThing1/ubuntu-wordpress/blob/main/ubuntu-wordpress.txt







>> --- Ubuntu setup ---
(UP and DOWN arrows to you-know-what, ENTER to progress, SPACE to confirm)

during install, language should be English. Press ENTER -> Done.
Keep keyboard layout the same. Done.
Leave Ubuntu server checked. Done.
Keep IPv4 static. Just press Done.
No need for proxy, just skip this one (Done).
Once repositories are listed (basically when it displays "Reading Package Lists..."), press Done.
   [Root partition] Custom Storage Layout -> Free Space -> Add GPT Partition -> 25G; Mount: / -> Create.
   [Home partition] Free Space -> Add GPT Partition -> 7G; Mount: /home -> Create.
   [Physical] Add GPT Partition -> 6.9G; Format: Swap -> Create.
Once all three partitions are made, tab and press Done; then Continue.
Name: "your name" -> ServerName: "your name"server1 -> username: student -> password: 1234 -> Once all spaces are filled, press Done.
Do not activate Ubuntu Pro, just continue.
Turn on Install OpenSSH Server before pressing Done.
Skip services (server snaps), just press Done; Then wait for a good while for it to install before it prompts you to continue (it will display Reboot Now).
When Reboot Now is listed, just press it; Once it prompts you to "remove installation medium", just press ENTER, as this is just a virtual machine.

login (aka username): student; password: 1234 (as we have set up)

>> --- Commands to eventually set up desktop experience ---
sudo -s (password 1234) (takes us to /home)
apt update
apt upgrade -y
ifconfig -> apt install net-tools -y
ifconfig (checking inet)
apt install mc -y
mc (why not; this isn't really necessary. If you still do decide to enter, just press F10 to leave)

>> --- Set up ubuntu desktop experience --- 
command: apt install ubuntu-desktop -y
once installed, type reboot; once rebooted, just enter the system as normal. password is 1234 as stated many times before.
First two phases done. Now move on to setting up wordpress.

------------------------------------------------------------
------------------------------------------------------------
------------------------------------------------------------
>> --- Wordpress setup ---

>> --- Update system ---
sudo apt update && sudo apt upgrade -y

>> --- Install Apache, PHP, MySQL ---
sudo apt install apache2 mysql-server php libapache2-mod-php php-mysql wget -y

>> --- Download and extract WordPress ---
cd /tmp
wget https://wordpress.org/latest.tar.gz
tar -xvzf latest.tar.gz

>> Move WordPress into Apache web root
(most likely directory won't exist, so type in: mkdir wordpress)
sudo mv wordpress /var/www/html/

>> Set permissions
sudo chown -R www-data:www-data /var/www/html/wordpress
sudo chmod -R 755 /var/www/html/wordpress

>> --- Configure MySQL database ---
sudo mysql -u root -p <<EOF
DROP USER IF EXISTS 'wpuser'@'localhost';
CREATE DATABASE wordpress;
CREATE USER 'wpuser'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON wordpress.* TO 'wpuser'@'localhost';
FLUSH PRIVILEGES;
EXIT;
EOF


> if that does not work, just paste in this entire thing:
sudo mysql -u root -p
 
-- Check if the user exists
SELECT User, Host FROM mysql.user WHERE User = 'wpuser';
 
-- Drop the user if it exists
DROP USER IF EXISTS 'wpuser'@'localhost';
 
-- Create database and user
CREATE DATABASE wordpress;
CREATE USER 'wpuser'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON wordpress.* TO 'wpuser'@'localhost';
FLUSH PRIVILEGES;
 
EXIT;


>> --- Create SSL directory and generate self-signed certificate ---
sudo apt install openssl -y
sudo mkdir -p /etc/apache2/ssl
sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout /etc/apache2/ssl/apache.key \
  -out /etc/apache2/ssl/apache.crt

>> --- Edit Apache SSL config (default-ssl.conf) ---
sudo nano /etc/apache2/sites-available/default-ssl.conf
Insert inside <VirtualHost _default_:443>:
SSLEngine on
SSLCertificateFile /etc/apache2/ssl/apache.crt
SSLCertificateKeyFile /etc/apache2/ssl/apache.key
(Once finished, press CTRL+T -> CTRL+Z)

>> --- Enable SSL in Apache ---
sudo a2enmod ssl
sudo a2ensite default-ssl
sudo systemctl reload apache2

> sudo a2enmod ssl && sudo a2ensite default-ssl && sudo systemctl reload apache2

>> --- Test access ---
> type in ifconfig to check your IP just in case you forget like me (usually the IP of the vm is 127.0.0.1).

> HTTP:
    http://<your_VM_IP>/wordpress
> HTTPS:
    https://<your_VM_IP>/wordpress
